These are the PBL scripts Ned shared with Dave.

in a Apr 13, 2026 email: 
    [Ned] did not include everything required for the scripts to run successfully (ie. eliminated all the non-CM1 datasets, but the codes still expect those datasets to be located in a certain location).  He also altered the directory structure slightly, so the codes will point at directories that don't necessarily exist (relatively easy to remedy).

via [Google Drive](https://drive.google.com/drive/folders/17hq0sxLF85cNlxLFRTYBVH-_Ap3OrPdC?usp=sharing)

In a May 11, 2026 email, Ned shared `cm1/shared_by_patton/src/read_les_cm1.py` so CM1_PBLH could be defined.